package ch.so.agi;

import org.gradle.api.Plugin;
import org.gradle.api.Project;

public class InterlisRepositoryCreatorPlugin implements Plugin<Project> {
    @Override
    public void apply(Project project) {}
}
